import React from "react";
import {Navbar}  from '../Navbar/Navbar';
import {Container} from '../Container/Container';

export const Main = () => {
    return (
        <div>
        <Navbar/>
        <Container></Container>
        </div>
    )
}